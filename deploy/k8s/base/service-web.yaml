apiVersion: v1
kind: Service
metadata:
  name: tally-web
  namespace: lurus-tally
  labels:
    app: tally-web
spec:
  type: ClusterIP
  selector:
    app: tally-web
  ports:
    - name: http
      port: 3000
      targetPort: 3000
      protocol: TCP
