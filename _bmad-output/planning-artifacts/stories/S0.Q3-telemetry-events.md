# Story S0.Q3: Telemetry event union 扩 5 个 + NATS PSI_TELEMETRY 发布

Status: ready-for-dev

**Sprint:** 0 (Foundation Week)
**Track:** Q (observability)
**Feature:** S0-Q3 — 扩展前端 telemetry → NATS
**Owner:** full-stack
**Repo:** `2b-svc-psi`
**Estimate:** 10h
**Blockers:** 无（`web/lib/telemetry.ts` + `web/app/api/otel-events/route.ts` 已存在）

## Story

As a product analyst,
I want 5 new typed telemetry events (`palette_invocation`、`ai_drawer_open`、`plan_accept_rate`、`onboarding_first_po_exported`、`cmd_z_used`) 写到 NATS `PSI_TELEMETRY.web.*`,
so that Sprint 1-12 的"⌘K DAU ≥60%"、"AI Drawer 相关性"、"onboarding 30 min"、"Cmd+Z 月 5 次"等量化目标能从前端真实埋点 aggregator 出来。

## Acceptance Criteria

1. `web/lib/telemetry.ts:8` `TelemetryEvent` union 从 2 扩到 7：保留 `draft_restore` / `undo_used`，新增 `palette_invocation` / `ai_drawer_open` / `plan_accept_rate` / `onboarding_first_po_exported` / `cmd_z_used`。
2. 每个 event 的 metadata 字段 schema 文档化在 `web/lib/telemetry.ts` JSDoc：
   - `palette_invocation`: `{latency_ms, query_chars, action_picked: 'navigate|query|execute|none'}`
   - `ai_drawer_open`: `{page_context, trigger: 'shortcut|button|deeplink'}`
   - `plan_accept_rate`: `{plan_id, kind: 'replenishment|movement|...', accepted: '1|0'}`
   - `onboarding_first_po_exported`: `{tenant_age_minutes, export_format: 'csv|pdf'}`
   - `cmd_z_used`: `{entity_type, undo_latency_ms}`
3. `web/app/api/otel-events/route.ts` 接收 POST 后，**除了**写 OTel（如已有）**额外**用 NATS publisher 发到 subject `PSI_TELEMETRY.web.<event_name>`；payload = `{tenant_id, user_id, event, metadata, timestamp}` JSON。
4. NATS publisher 调用后端新 endpoint `POST /internal/v1/telemetry/web` 转发（前端不直连 NATS）；endpoint 走 same-origin cookie auth；后端用现有 `internal/adapter/nats/publisher_typed.go` 加新 typed method `PublishWebTelemetry`。
5. Playwright e2e test：5 个新 event 全部触发一次，断言 NATS subject `PSI_TELEMETRY.web.>` consumer 收到 5 条对应 payload。
6. 不破坏现有 `draft_restore` / `undo_used` 流；现有调用点（如 `restoreSnapshot` 等）回归测试通过。

## Tasks / Subtasks

- [ ] 扩 `web/lib/telemetry.ts:8` union + 加 JSDoc + 加 `Metadata<E>` 泛型类型让 metadata 类型安全 (AC: 1, 2) — **2h**
- [ ] 后端新建 `internal/adapter/handler/telemetry/handler.go` + `POST /internal/v1/telemetry/web`；validate event 名 ∈ allow-list（避免任意字符串污染 NATS）(AC: 4) — **2.5h**
- [ ] 在 `internal/adapter/nats/publisher_typed.go` 加 `PublishWebTelemetry(ctx, payload)` 方法，subject 模板 `PSI_TELEMETRY.web.<event_name>` (AC: 3, 4) — **1.5h**
- [ ] `web/app/api/otel-events/route.ts` 调 backend `/internal/v1/telemetry/web` 转发；保留原 OTel 写入（如已有） (AC: 3) — **1h**
- [ ] Playwright e2e `web/tests/telemetry-events.spec.ts`：5 event 触发；用 NATS test consumer (`nats sub 'PSI_TELEMETRY.web.>'`) 录 5 条 (AC: 5) — **2.5h**
- [ ] 文档：在 `doc/coord/contracts.md` 加 NATS subject 段 `PSI_TELEMETRY.web.*` schema (AC: 1-4) — **0.5h**

## Files To Touch

EDIT:
- `web/lib/telemetry.ts` (扩 union + Metadata 泛型 + JSDoc)
- `web/app/api/otel-events/route.ts` (转发 POST 到 backend)
- `internal/adapter/nats/publisher_typed.go` (加 PublishWebTelemetry)
- `internal/adapter/handler/router/router.go` (注册新 endpoint)
- `doc/coord/contracts.md` (subject 段)

NEW:
- `internal/adapter/handler/telemetry/handler.go`
- `internal/adapter/handler/telemetry/handler_test.go`
- `web/tests/telemetry-events.spec.ts`

## Test Plan

### Unit
- `internal/adapter/handler/telemetry/handler_test.go`：event 名 allow-list 阻挡 unknown / 5 个合法 event publish 成功 / 缺 tenant_id → 401

### E2E (Playwright)
- 注册→走 onboarding 流→点 ⌘K→开 Drawer→accept plan→export PO→Cmd+Z，每步触发对应 event；NATS consumer 录满 5 条
- 在 CI 跑 `bun playwright test telemetry-events.spec.ts`

### 手测
- `nats sub 'PSI_TELEMETRY.web.>'` 一边监听
- 浏览器手按 5 个动作，看 NATS 实时收 5 条

## Dependencies

- NATS stream `PSI_EVENTS` 已存在；本 story 用同 server 新 subject `PSI_TELEMETRY.web.*`（如果 stream 是单 subject 配置需扩 subject filter — 见 Dev Notes）
- `internal/adapter/nats/publisher_typed.go` 已有 typed publisher pattern

## Dev Notes

- **NATS subject vs stream**：现 `PSI_EVENTS` stream 大概率只接 `PSI_EVENTS.>`。新增 `PSI_TELEMETRY.>` 需要：1) 在 stream 配置加 `Subjects: ["PSI_EVENTS.>", "PSI_TELEMETRY.>"]` 或 2) 新建独立 `PSI_TELEMETRY` stream（推荐 2，避免业务/遥测混 retention）。Dev 实施前 `nats stream info PSI_EVENTS` 确认。
- **PII 守卫**：metadata 字段 allow-list 严格 — 不接受 `user_email` / `note` 等自由文本（避免日志泄漏）。
- 前端 `trackEvent` 仍 fire-and-forget；NATS publish 失败不能 throw 给 UI。
- backend handler 失败时返 204（不阻塞 UI）+ structured log `WARN` 让 ops 排查。
- 不要在前端直接连 NATS — websocket NATS 暴露 server 不在本 sprint 范围。

### References

- [Source: roadmap-v1.5.md#Sprint-0 S0-Q3]
- [Source: web/lib/telemetry.ts:8 current union]
- [Source: web/app/api/otel-events/route.ts current handler]
- [Source: internal/adapter/nats/publisher_typed.go pattern]

## Dev Agent Record

### Agent Model Used

### Debug Log References

### Completion Notes List

### File List
